name =[]
balance = []
name= none

print("Enter the names and balances of bank accounts(tyoe: quit shen done)")

while name != "quit":
    name = float(input("What is the account? "))
       
    if name!= "quit"
       balance = float(input("What is the balance? "))
       accounts.append(name)
       balances.append(balance)
       print(f"{name}")

print("\nAccount Information")
